#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void)
{
	double x, y;
	double answer;

	answer = x + y;

	scanf ("%lf", &x);
	scanf ("%lf", &y);

	printf("your answer is: %.3lf", answer);

	return (0);
}
